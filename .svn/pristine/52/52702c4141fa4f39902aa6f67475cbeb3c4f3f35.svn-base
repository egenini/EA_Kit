using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DMN.dominio
{
    /// <summary>
    /// Recibe la definición de los datos de entrada y los convierte en parámetros, a la inversa con las conclusiones.
    /// Gestiona la/las decisones asociadas.
    /// Si se genera desde un diagrama las decisiones del primer nivel tienen que ser las que tengan el atributo isOutput = "1"
    /// </summary>
    public class Gestor
    {
        public Dictionary<string, ContenedorDato> contenedoresDatos = new Dictionary<string, ContenedorDato>();
        public Dictionary<string, Decision> decisiones = new Dictionary<string, Decision>();


    }
}
